var stateFadeDelay = <%= Rails.env == 'test' ? 1: 1500 %>;
var showState = function() {
  $('#state')
    .fadeIn('fast')
    .delay(stateFadeDelay)
    .fadeOut('fast', function() {
      $(this).hide();
    });
}
