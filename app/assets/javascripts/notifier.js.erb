var notifierDelay = <%= Rails.env == 'test' ? 100 : 2000 %>;

var notifyForChunk = function(gcid, message) {
  $('#' + gcid + ' .chunk-indicator')
    .show()
    .fadeIn('fast')
    .delay(notifierDelay)
    .fadeOut('fast', function() { $(this).hide() });
};
