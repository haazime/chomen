$('#chunk_gpid').val("<%= @page.gpid %>");
$('#chunk_id').val("<%= @page.chunk.id %>");
$('#page-list').html("<%=j render(partial: 'pages/list', locals: { pages: @pages }) %>");
$('#page-count').text(<%= @pages.size %>);

showState();
pageListHandler();
autoSaveHandler();
