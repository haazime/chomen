$('#page-list').replaceWith("<%=j render(partial: 'pages/list', locals: { pages: @pages }) %>");
$('#page-count').text(<%= @pages.size %>);

$('#<%= @page.chunk.gcid %> .chunk-actions')
  .first()
  .html("<%=j render(partial: 'chunks/actions', locals: { chunk: @page.chunk }) %>");

showState();
