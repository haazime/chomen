<div class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="nav-header">
      <%= link_to 'Chomen', root_url, class: 'navbar-brand' %>
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="navbar-collapse collapse" id="navbar-main">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <%= fa_icon('list-alt lg') %>
          </a>
          <ul id="page-list" class="dropdown-menu">
            <%= render partial: 'pages/recent_list', locals: { pages: @recent_pages } %>
          </ul>
        </li>
        <li>
          <%= link_to new_page_url do %>
            <%= fa_icon('pencil-square-o lg') %>
          <% end -%>
        </li>
        <li>
          <% if @page.present? -%>
            <%= link_to new_chunk_url(gpid: @page.gpid), remote: true, id: 'chunk-adder' do %>
              <%= fa_icon('plus-circle lg') %>
            <% end -%>
          <% end -%>
        </li>
      </ul>
    </div>
  </div>
</div>
